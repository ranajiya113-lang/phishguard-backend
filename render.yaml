services:
  - type: web
    name: phishguard-backend
    runtime: python
    pythonVersion: "3.11.0"
    buildCommand: pip install fastapi uvicorn[standard] httpx scikit-learn numpy pydantic python-multipart
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
